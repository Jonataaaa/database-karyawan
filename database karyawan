import mysql.connector

db = mysql.connector.connect(
    host='localhost',
    user='root',
    password='',
    database=''
)

cursor = db.cursor()

cursor.execute("CREATE DATABASE IF NOT EXISTS dbkaryawan")

cursor.close()
db.close()
